// condition6exe.js
// 1. 두 수를 입력. 큰 값을 콘솔에 출력. ex)입력된 두 값이 17, 28일 경우 "두 수중에서 큰 값은 28"

// 2. 임의의(Math.random) 두 개의 수(범위: 40 ~ 70)를 생성. "2개의 수중에서 큰 값은 ??, 작은값은 ??"

// 3. condition3.js에 "A", "B", "C", "D", "F" 중 95점 이상이면 A+, 85이상이면 B+, 75이상이면 C+, 65이상이면 D+

// 4. 반복문(537페이지 연습.)---> 미리 예습해오기.
